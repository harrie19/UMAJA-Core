import React from 'react'
import ReactDOM from 'react-dom/client'
import { RealityLabUI } from './3d/RealityLabUI'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RealityLabUI />
  </React.StrictMode>,
)
